import { Routes } from '@angular/router';
import { FlowManagerComponent } from '../flow/presentation/pages/flow-manager/flow-manager.component';


export const routes: Routes = [
    {
        path: '',
        component: FlowManagerComponent,
    },
];
